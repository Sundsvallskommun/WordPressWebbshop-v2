<div class="wrap">
	<h1>SMEX-inställningar</h1>
	<form method="post" action="options.php">
		{{ function( 'settings_fields', 'smex-options-group' ) }}

		<table class="form-table">
			<tr valign="top">
				<th scope="row">URL</th>
				<td>
					<input type="text" name="smex_api_url" value="{{ smex_api_url }}" />
					<p class="description">Adress till SMEX-katalogen.</p>
				</td>
			</tr>
		</table>

		{{ function( 'submit_button' ) }}
	</form>
</div>